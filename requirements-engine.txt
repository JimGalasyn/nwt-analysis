# Field-theory upstreams for the nwt-analysis[engine] drivers, pinned to
# DOI-backed release tags. PyPI forbids direct-reference (git URL) deps in
# published package metadata, so they live here instead of in pyproject's extra.
# Until nwt-substrate / jax-solitons publish to PyPI, install them with:
#     pip install -e . -r requirements-engine.txt
nwt-substrate @ git+https://github.com/JimGalasyn/nwt-substrate.git@v0.5.0
jax-solitons @ git+https://github.com/JimGalasyn/jax-solitons.git
